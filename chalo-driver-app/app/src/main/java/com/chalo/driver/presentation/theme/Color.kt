package com.chalo.driver.presentation.theme

import androidx.compose.ui.graphics.Color

// Brand — saffron orange (same brand identity as customer app)
val ChaloPrimary        = Color(0xFFF95C00)   // saffron orange
val ChaloPrimaryDark    = Color(0xFFE55A00)   // pressed state
val ChaloOnPrimary      = Color(0xFFFFFFFF)

// Driver app accent: navy (professional driver feel)
val ChaloSecondary      = Color(0xFF0F1629)   // deep navy
val ChaloOnSecondary    = Color(0xFFFFFFFF)

val ChaloBackground     = Color(0xFFF8F8F8)
val ChaloSurface        = Color(0xFFFFFFFF)
val ChaloOnBackground   = Color(0xFF1A1A1A)
val ChaloOnSurface      = Color(0xFF1A1A1A)

val ChaloError          = Color(0xFFD32F2F)
val ChaloOnError        = Color(0xFFFFFFFF)

val ChaloSuccess        = Color(0xFF34D399)   // green (earnings, online)
val ChaloWarning        = Color(0xFFF57C00)

// Status colors
val StatusOnline        = Color(0xFF34D399)   // green
val StatusOffline       = Color(0xFF9CA3AF)   // gray
val StatusBusy          = Color(0xFFF59E0B)   // amber (on ride)

// Ride status colors
val StatusSearching     = Color(0xFFFFA726)
val StatusAssigned      = Color(0xFF42A5F5)
val StatusArrived       = Color(0xFF66BB6A)
val StatusInProgress    = Color(0xFF26C6DA)
val StatusCompleted     = Color(0xFF4CAF50)
val StatusCancelled     = Color(0xFFEF5350)

// Rating star
val StarColor           = Color(0xFFFFB300)

// Text shades
val TextPrimary         = Color(0xFF1A1A1A)
val TextSecondary       = Color(0xFF757575)
val TextHint            = Color(0xFFBDBDBD)

// Divider / border
val DividerColor        = Color(0xFFE0E0E0)

// Map
val RoutePrimary        = Color(0xFFF95C00)
